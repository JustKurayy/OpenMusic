Create a full-stack Spotify alternative music web application using Node.js (Express) for the backend and React for the frontend. The app should allow users to:

⸻

🎵 Core Features:
	•	Upload and play .mp3 and .wav audio files.
	•	Stream uploaded audio in the browser.
	•	Create, update, delete, and view playlists.
	•	Add or remove songs from playlists.
	•	View and browse their own music library.

⸻

🔐 Authentication:
	•	Implement OAuth2 login (e.g., Google OAuth2).
	•	Use JWT (JSON Web Tokens) for secure session handling.
	•	Users should only access their own playlists and media.

⸻

📁 Backend (Node.js + Express):
	•	Use Express.js for RESTful API endpoints:
	•	/api/auth for OAuth2 JWT auth flow.
	•	/api/tracks to upload, stream, and list audio files.
	•	/api/playlists to manage playlists.
	•	Use Multer for file uploads and store audio files locally (or stub a cloud option).
	•	Audio metadata (title, duration) is extracted and stored in a database.
	•	Database: Use MongoDB or PostgreSQL.

⸻

🎧 Frontend (React):
	•	Use modern React (with Hooks) and React Router.
	•	Clean UI inspired by Spotify:
	•	Sidebar for navigation (Home, Library, Playlists, Upload).
	•	Main area for displaying tracks and playlists.
	•	Top bar with account/login info.
	•	Music player footer (play/pause, skip, volume).
	•	Audio player component with:
	•	Playback controls.
	•	Track progress bar.
	•	Volume control.
	•	Pages:
	•	Home: Recent and popular tracks.
	•	Library: Uploaded songs.
	•	Playlist: User-created playlists and ability to play them.
	•	Upload: Upload .mp3 or .wav tracks.

⸻

📦 Additional Features (Optional but Encouraged):
	•	Dark mode toggle (Spotify-style dark theme).
	•	Album art support.
	•	Search functionality (by track name, artist).
	•	Drag and drop reordering in playlists.
	•	Queue system (play next, shuffle).
	•	Mobile responsive layout.

⸻

🛠️ Tech Stack:
	•	Frontend: React, React Router, Axios, TailwindCSS or styled-components.
	•	Backend: Node.js, Express.js, Multer, JWT, Passport.js (for OAuth2).
	•	Database: MongoDB or PostgreSQL (with Prisma or Mongoose).
	•	Audio streaming: Use HTML5 <audio> with Blob URLs.

⸻

✅ Deliverables:
	•	Full working app with login, music upload, playback, and playlists.
	•	Source code organized into frontend and backend folders.
	•	.env.example for environment configuration.
	•	README.md with setup instructions.